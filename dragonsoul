local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
    Name = "TheRsHub | [UPD🔥] Clicker Fighting Simulator",
    LoadingTitle = "TheRsHub",
    LoadingSubtitle = "by TheRsbx",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = nil,
        FileName = "TheRsHub"
    },
    Discord = {
        Enabled = true,
        Invite = "FFyKPGUdAU",
        RememberJoins = true -- Set this to false to make them join the discord every time they load it up
    },
    KeySystem = true,
    KeySettings = {
        Title = "[UPD🔥] Clicker Fighting Simulator",
        Subtitle = "TheRsHub Key System",
        Note = "Join the Discord to get the Key | dsc.gg/thershub",
        FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
        SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
        GrabKeyFromSite = true, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
        Key = {"Hi"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
    }
})

-- VALUES
_G.AutoTap = true

-- FUNCTIONS


-- TAB AUTOFARM --
local Tab = Window:CreateTab("Teleport", "nil")
local Section = Tab:CreateSection("Map")

-- TELEPORT 1 --

local Button = Tab:CreateButton({
    Name = "Starter Island ( Jungle )",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-1145, 371, -3007),
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
                -- a.Completed:Wait()
                -- print('Teleporting Done!')
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })

 -- TELEPORT 2 --

 local Button = Tab:CreateButton({
    Name = "Great Plains",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-1112, 397, -6238), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
                -- a.Completed:Wait()
                -- print('Teleporting Done!')
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })


-- TELEPORT 4 --

 local Button = Tab:CreateButton({
    Name = "Desolate Lands of Apes",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-1457, 487, -9907), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })



-- TELEPORT 5 --

local Button = Tab:CreateButton({
    Name = "East City",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-2694, 681, 4388), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })



-- TELEPORT 6 --
local Button = Tab:CreateButton({
    Name = "Red Ribbon",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(2779, 589, -2499), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })



-- TELEPORT 7 --
local Button = Tab:CreateButton({
    Name = "City",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-3873, 681, -4798), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })


-- TELEPORT 8 --

local Button = Tab:CreateButton({
    Name = "Grand Patrol",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-4510, 564, -289), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })


-- TELEPORT 9 --

local Button = Tab:CreateButton({
    Name = "Power Tower Island",
    Callback = function()
        local teleport_table = {
            location1 = Vector3.new(-8017, 837, 8218), 
        }
        
        local tween_s = game:GetService('TweenService')
        local tweeninfo = TweenInfo.new(1,Enum.EasingStyle.Linear)
        
        local lp = game.Players.LocalPlayer
        
        function bypass_teleport(v)
            if lp.Character and 
            lp.Character:FindFirstChild('HumanoidRootPart') then
                local cf = CFrame.new(v)
                local a = tween_s:Create(lp.Character.HumanoidRootPart,tweeninfo,{CFrame=cf})
                a:Play()
            end
        end
        
        bypass_teleport(teleport_table.location1)
    end,
 })
